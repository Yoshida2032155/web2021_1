 <!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>VALORANTエージェント一覧</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.name %></td>
            <td><%= row.offense %></td>
            <td><%= row.defense %></td>
            <td><%= row.search %></td>
            <td><%= row.control %></td>
            <form action="/001">
            <td>
              <input type="submit" name="<%= row.id %>" value="決定">
            </td>
            </form>
        </tr>
      <% } %>
      <td>
        <a href="/views/pick.ejs">編成確認</a>
      </td>
  </table>
</body>
</html>